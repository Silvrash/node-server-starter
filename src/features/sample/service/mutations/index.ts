import { AddSampleSchema, UpdateSampleSchema, Sample } from '@features/sample';
import { getSampleById } from '../queries';
import R from 'ramda';

export const addSample = async (data: AddSampleSchema) => {
};

export const updatePoll = async (id: string, data: UpdateSampleSchema) => {

};

export const deletePoll = async (id: string) => {
};
